{
  "$comment": "Public exports intentionally NOT demonstrated in apps/ yet. Each entry needs { symbol, reason, issue }. The list is empty: every public export — including the browser-only ./react hooks (useOtpInput, useOtpCountdown) and their types, now consumed by apps/web's OTP panel — is referenced in apps/. Never use this list to make CI green for a real, demonstrable export.",
  "ignored": []
}
